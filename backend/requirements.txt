# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Persistence / geospatial ---
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
geoalchemy2==0.15.2
shapely==2.0.6

# --- Validation / config ---
pydantic==2.9.2
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- ML layer (forecast + anomaly detection) ---
scikit-learn==1.5.2

# --- GenAI provider calls (runtime dependency, not dev-only) ---
httpx==0.27.2

# --- Dev / test ---
pytest==8.3.3
ruff==0.6.9
black==24.8.0
pre-commit==3.8.0
